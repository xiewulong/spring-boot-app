# Eureka
# eureka.client.fetch-registry = true
# eureka.client.register-with-eureka = true
# eureka.client.service-url.defaultZone = http://${eureka.instance.hostname}:${server.port}/eureka/
# eureka.instance.hostname =
# eureka.instance.ip-address =
# eureka.instance.non-secure-port = 80
# eureka.instance.prefer-ip-address = false
# eureka.server.enable-self-preservation = true

# Feign
feign.compression.request.enabled = true
feign.compression.response.enabled = true
feign.httpclient.enabled = true

# Flyway
flyway.table = t_schema_version

# Security
security.basic.enabled = false
# security.user.name = user
# security.user.password =

# Server
# server.port = 8080

# Spring application
spring.application.name = demo
spring.application.route.prefix = /${spring.application.name}
spring.application.swagger.enabled = true

# Spring datasource
spring.datasource.url = jdbc:mysql://localhost:3306/test
spring.datasource.username = root
# spring.datasource.password =

# Spring message source
spring.messages.basename = i18n/messages

# Spring profiles
# spring.profiles.active =

# Springfox
# springfox.documentation.swagger.v2.path = ${spring.application.route.prefix}/v2/api-docs

# Zuul
# zuul.add-host-header = false
# zuul.ignored-services = *
# zuul.prefix =
# zuul.strip-prefix = true

# Zuul - a
# zuul.routes.a = /*/a/**
# zuul.routes.a.strip-prefix = true

# Zuul - b
# zuul.routes.b.path = /*/b/**
# zuul.routes.b.service-id = b
# zuul.routes.b.strip-prefix = true

# Zuul - c
# zuul.routes.c.path = /*/c/**
# zuul.routes.c.strip-prefix = true
# zuul.routes.c.url = http://c.domain.com

# Zuul - d
# zuul.routes.c.path = /*/d/**
# zuul.routes.d.strip-prefix = true
# zuul.routes.c.url = forward:/c
